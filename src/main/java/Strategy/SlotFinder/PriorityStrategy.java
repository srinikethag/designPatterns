package Strategy.SlotFinder;

public class PriorityStrategy implements SlotFinderStrategy{

    @Override
    public Slot findSlot(Vehicle vehicle) {
        // find slot on priority
        return new Slot();
    }
}
